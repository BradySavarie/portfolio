import { Typography, Box } from '@mui/material';

export default function About() {
  return (
    <Box sx={{ outline: '1px solid black', height: '768px' }}>
      <Typography>Test</Typography>
    </Box>
  );
}
